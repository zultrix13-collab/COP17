# Master QA Audit — 2026-06-16

## Зорилго
Build 36 Apple rejection-ийн үндэс шалтгааныг (uncommitted working-tree code) давтуулахгүй байхын тулд бүх codebase-ийг шалгав.

---

## 🔴 CRITICAL — Олдсон, засагдсан

### 1. reviewSession bypass — зөвхөн 3 файлд commit хийгдсэн байсан
`env.dart`, `email_page.dart`, `otp_page.dart` commit `5b709f3`-д хийгдсэн боловч дараах файлуудад **үгүй байсан**:

| Файл | Асуудал | Үр дагавар |
|------|---------|-----------|
| `router.dart` | `reviewSession` check байхгүй | Reviewer `/home` руу орсон хойноо redirect-д баригдана |
| `profile_repository.dart` | `reviewSession` → demoProfile байхгүй | Reviewer хоосон profile харна |
| `programme_repository.dart` | `reviewSession` → demoSessions байхгүй | Reviewer хоосон programme харна |

**Fix:** commit `90118d6` — бүгдийг нэмлээ + `reviewer_bypass_test.dart`

### 2. Theme refactoring — compile-breaking uncommitted state
`CopRadius→SiopRadius`, `buildCop17Theme→buildSiopTheme`, `copBrandGradient→siopBrandGradient` нь `theme.dart`-д uncommitted байсан. Харин `home_page.dart`, `splash_page.dart`, `digital_id_page.dart`, `main.dart` нь шинэ нэрийг ашиглаж байсан.

**Үр дагавар:** Clean git checkout хийвэл `flutter build` **compile error** гарна.

**Fix:** commit `91b3232`

### 3. dart_defines.json — secrets in working tree, .gitignore-д байхгүй
Supabase URL + anon key агуулсан файл `.gitignore`-д байгаагүй → санамсаргүй commit болж мэдэх.

**Fix:** commit `54a8834` — `.gitignore`-д нэмлээ, `dart_defines.json.example` template үүсгэлээ

---

## 🟡 IMPORTANT — Олдсон, засагдсан

### 4. Build-critical untracked files
| Файл | Зачем нь |
|------|---------|
| `ios/ExportOptions.plist` | `flutter build ipa` export-д шаардлагатай |
| `ios/Runner/PrivacyInfo.xcprivacy` | Apple Privacy Manifest (шаардлагатай) |
| `lib/core/widgets/error_view.dart` | App-д ашиглагдаж байгаа widget |

**Fix:** commit `54a8834` — бүгдийг tracked болгосон

### 5. Admin-web + Supabase rebrand uncommitted
`index.html`, `locales/`, `config.toml`, `templates/otp.html`, `seed.sql` — "COP17" strings хэвээр байсан.

**Fix:** commit `3431c94`

---

## 🟢 LOW — Санамж

### 6. android/ directory untracked
Play Store build-д хэрэгтэй Android platform directory хаана ч commit хийгдээгүй байна. Keystore file (`upload-keystore.jks`) `.gitignore`-д нэмэгдсэн.

### 7. Test coverage — reviewer bypass
`test/` folder untracked байсан. Одоо `reviewer_bypass_test.dart` нэмэгдлээ:
- `reviewCode` is exactly 6 digits, numeric
- `reviewSession` starts `false`

---

## Давтагдахгүй байхын тулд — дүрэм

1. **Build хийхээс өмнө `git diff --stat HEAD` шалга** — 0 modified lines байх ёстой
2. **Шинэ bypass/auth нэмэхдээ** router + data repositories + tests бүгдийг хамтад нь commit хий
3. **Secrets (`dart_defines.json`, keystore)** `.gitignore`-д байгаа эсэхийг шалга
4. **`flutter build ipa`** ажиллахад шаардлагатай бүх файл (`ExportOptions.plist` гэх мэт) git-д байх ёстой

---

## Commit summary

| Commit | Агуулга |
|--------|---------|
| `90118d6` | fix: reviewSession → router + repositories + test |
| `91b3232` | refactor: CopRadius→SiopRadius (compile fix) |
| `54a8834` | chore: ExportOptions, error_view, tests, .gitignore secrets |
| `3431c94` | chore: admin-web + supabase rebrand |
| `14cbf90` | docs: go-live checklist + session docs |
| `06e69fd` | chore: PWA manifest rebrand |
