// Auto-mirrorable from index.json. Keep in sync with the JSON source of truth.
import 'package:flutter/material.dart';

class DesignTokens {
  // Tier colors
  static const tierGreen     = Color(0xFF16A34A);
  static const tierBlue      = Color(0xFF1A6EF5);
  static const tierVip       = Color(0xFF7C3AED);
  static const tierExhibitor = Color(0xFFB45309);
  static const tierPress     = Color(0xFF0369A1);

  // Semantic
  static const background = Color(0xFFF0F0F0);
  static const surface    = Color(0xFFFFFFFF);
  static const text       = Color(0xFF111111);
  static const muted      = Color(0xFF888888);
  static const border     = Color(0xFFE0E0E0);
  static const success    = Color(0xFF16A34A);
  static const warning    = Color(0xFFB45309);
  static const danger     = Color(0xFFDC2626);
  static const info       = Color(0xFF1A6EF5);

  // Spacing
  static const sXs = 4.0;
  static const sSm = 8.0;
  static const sMd = 12.0;
  static const sLg = 16.0;
  static const sXl = 24.0;
  static const s2xl = 32.0;

  // Radius
  static const rSm = 5.0;
  static const rMd = 8.0;
  static const rLg = 10.0;
  static const rXl = 20.0;
}
