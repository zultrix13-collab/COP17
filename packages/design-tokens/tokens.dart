// Mirror of index.json. Keep in sync manually until a codegen step is added.
import 'package:flutter/material.dart';

class DesignTokens {
  // Brand
  static const primary = Color(0xFF14464F); // Logo teal
  static const sky     = Color(0xFF05B6C4); // Eternal Blue Sky
  static const sand    = Color(0xFFF5D9A8); // Gobi dune

  // Tiers
  static const tierGreen     = Color(0xFF059669);
  static const tierBlue      = Color(0xFF05B6C4);
  static const tierVip       = Color(0xFF8B5CF6);
  static const tierExhibitor = Color(0xFFD97706);
  static const tierPress     = Color(0xFF0369A1);

  // Semantic
  static const ink        = Color(0xFF1F2937);
  static const inkMuted   = Color(0xFF6B7280);
  static const border     = Color(0xFFE5E7EB);
  static const bg         = Color(0xFFF9FAFB);
  static const surface    = Color(0xFFFFFFFF);
  static const surfaceAlt = Color(0xFFF3F4F6);
  static const success    = Color(0xFF059669);
  static const warning    = Color(0xFFD97706);
  static const danger     = Color(0xFFDC2626);
  static const info       = Color(0xFF05B6C4);

  // Spacing
  static const sXs = 4.0;
  static const sSm = 8.0;
  static const sMd = 12.0;
  static const sLg = 16.0;
  static const sXl = 24.0;
  static const s2xl = 32.0;

  // Radius
  static const rSm = 8.0;
  static const rMd = 12.0;
  static const rLg = 16.0;
  static const rXl = 20.0;
  static const rPill = 999.0;
}
